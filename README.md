# Assigment_3

Remember to have the node_modules needed for this paractice, if you dont have it just run: npm i artillery to install artillery only in the project.
To run the test use the command npm run test , it will create the reports and perform the test.
To use your personal token execute the command export TOKEN=¨your token here" to be able to run the tests
